# Enforce the Node floor declared in package.json "engines". Without this npm
# treats engines as advisory. Angular 22 needs ^22.22.3 || ^24.15.0 || >=26, and
# the machine default (24.4.1) is below the floor — this stops a contributor at
# install time instead of at a confusing runtime failure.
engine-strict=true
